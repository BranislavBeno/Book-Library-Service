spring:
  security:
    oauth2:
      client:
        registration:
          cognito:
            poolId: b-l-s
            clientId: spring-boot-application
            clientSecret: BZgLD9SHUSMSpcwufCl39ZlFO6u7tODg
            scope: openid
            clientName: spring-boot-application
            logoutUrl: http://localhost:8888
        provider:
          cognito:
            issuerUri: http://localhost:8888/realms/b-l-s

cloud:
  aws:
    credentials:
      access-key: foo
      secret-key: bar

custom:
  use-cognito-as-identity-provider: false
